services:
    finsight-ai:
        container_name: finsight-ai-app
        build: 
            context: .
            dockerfile: Dockerfile
        ports: 
            - "3001:3001"
        env_file:
            - .env