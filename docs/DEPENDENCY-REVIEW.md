### Dependency Review

Pull requests are automatically checked for dependency changes using
GitHub Dependency Review.

Newly introduced `HIGH` and `CRITICAL` vulnerabilities block the
dependency review check. Lower-severity vulnerabilities are reported
without blocking the pull request.